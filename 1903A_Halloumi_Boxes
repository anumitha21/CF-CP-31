
//read qn kido
#include<iostream>
#include<vector>
using namespace std;
 
int main(){
int x;
cin>>x;
for(int j=0;j<x;j++){
int n, k;
cin >> n >> k;
 
vector<int> a(n);
 
for(int i = 0; i < n; i++) {
    cin >> a[i];
}
 
if(k >= 2) {
    cout << "YES\n";
}
else {
 
    bool sorted = true;
 
    for(int i = 1; i < n; i++) {
        if(a[i] < a[i-1]) {
            sorted = false;
            break;
        }
    }
 
    if(sorted)
        cout << "YES\n";
    else
        cout << "NO\n";
}
}
return 0;
}